# Auth System

Этот репозиторий содержит полноценную систему аутентификации, состоящую из **клиента на Next.js** и **сервера на NestJS**.  
Система предоставляет регистрацию, вход в аккаунт, JWT-аутентификацию и управление профилем пользователя.

---

## 🖥️ Структура проекта

auth-system/
├── client/ # Фронтенд на Next.js
├── server/ # Бэкенд на NestJS
├── docker/ # (Опционально) файлы для Docker
├── .gitignore
├── README.md
└── package.json


---

## Клиент

Фронтенд построен на [Next.js](https://nextjs.org/) и [TypeScript](https://www.typescriptlang.org/).  
Он отвечает за пользовательский интерфейс, формы аутентификации и взаимодействие с API сервера.

### ⚡ Основные возможности

- Вход и регистрация пользователей
- JWT-аутентификация
- Редактирование профиля
- Адаптивный интерфейс (мобильный и десктоп)
- Drag-and-drop управление (если применимо)
- Обработка ошибок и уведомления

### 🛠️ Используемые технологии и библиотеки

- **Next.js** – React-фреймворк для SSR и SSG
- **TypeScript** – Типизированный JavaScript
- **React Hook Form** – Работа с формами
- **Zod** – Валидация форм
- **Redux Toolkit** – Управление состоянием
- **Framer Motion** – Анимации
- **Axios** – HTTP-запросы
- **TailwindCSS** – Стилизация
- **Prettier & ESLint** – Форматирование и проверка кода

### 🚀 Запуск локально

1. Установить зависимости:

bash
cd client
npm install

2. Установить зависимости:

npm run dev

3. Открыть http://localhost:3000
   в браузере.

### Запуск через Docker

# Сборка Docker-образа
docker build -t auth-client ./client

# Запуск контейнера
docker run -p 3000:3000 auth-client



## Сервер

Бэкенд построен на NestJS
и TypeScript
.
Он обеспечивает аутентификацию, управление пользователями и API для фронтенда.

### ⚡ Основные возможности

- **JWT**-аутентификация

- **Регистрация и вход пользователей**

- **Обновление профиля**

- **Валидация DTO через class-validator**

- **Централизованная обработка ошибок**

- **Поддержка CORS с настройкой через .env**

- **Swagger документация API**

### 🛠️ Используемые технологии и библиотеки

- **NestJS** – Фреймворк для бэкенда

- **TypeScript** – Типизированный код

- **class-validator & class-transformer** – Валидация DTO

- **@nestjs/jwt** – JWT аутентификация

- **cookie-parser** – Чтение cookies для аутентификации

- **Swagger** – Документация API

- **Axios** – HTTP-запросы (опционально)

- **Prettier & ESLint** – Форматирование и проверка кода

- **Docker** – Контейнеризация

### 🚀 Запуск локально

1. Установить зависимости:

cd server
npm install


2. Создать .env файл:

PORT=4000
JWT_SECRET=your_jwt_secret
CORS_ORIGIN=http://localhost:3000


3. Запустить сервер в режиме разработки:

npm run start:dev


4. Открыть http://localhost:4000/api
для Swagger документации.

🐳 Запуск через Docker
# Сборка Docker-образа
docker build -t auth-server ./server

# Запуск контейнера
docker run -p 4000:4000 --env-file ./server/.env auth-server
